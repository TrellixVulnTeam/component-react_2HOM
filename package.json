{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "component-library-issue",
  "version": "1.0.0",
  "scripts": {
    "build:component": "lerna run build --scope @malavshah9/component-lib --stream",
    "dev:app": "lerna run dev --scope web-app-1 --stream",
    "dev": "yarn build:component && yarn dev:app"
  },
  "license": "MIT",
  "repository": "git@github.com:malavshah9/component-react.git",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
